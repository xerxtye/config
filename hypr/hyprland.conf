# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,1920x1080@144,auto,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = kitty -e ranger
$guiFileManager = thunar
$browser = helium
$videoPlayer = vlc
$mainEditor = kitty -e nvim
$scrapEditor = gnome-text-editor
$menu = wofi -i --show drun --normal-window
$powermenu = ~/.config/hypr/scripts/powermenu.sh if wanna handle with keys


#################
### AUTOSTART ###
#################

exec = kill $(pgrep 'battery-alert') # battery alert kill existing
#exec = ~/.config/hypr/scripts/battery-alert.sh & # battery alert launch new
exec-once = hypridle # idle state listener
exec-once = waybar # panel
exec-once = dbus-daemon --session --address=unix:path=$XDG_RUNTIME_DIR/bus # need dbus
exec-once = gentoo-pipewire-launcher restart & disown # sound init 
exec-once = dbus-update-activation-environment DISPLAY # for some apps that need this shit
exec-once = mako # dbus is actually loads up mako to launch at the first notification, 
exec = gsettings set org.gnome.desktop.interface gtk-theme "Adwaita-dark"   # for GTK3 apps
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"   # for GTK4 apps
#exec-once = hyprpaper & # init a wallpaper
# The below command inits styling, random wallpaper and a waybar, as well as terminal and editor styling 
exec-once = ~/.config/hypr/scripts/wallpaper-carousel.sh # Both waybar and hyprpaper
exec-once = docker restart ba66d0144145


# FlClashX vpn client
exec-once = $HOME/Programs/FlClashX/usr/share/FlClashX/FlClashX 

# Vesktop
exec-once = $HOME/Programs/Vesktop/vesktop

# Adwaita Cursor
exec-once = hyprctl setcursor Adwaita 24


#############################
## DISABLED AUTOSTART APPS ##
#############################
#
# music daemon
# exec-once = mpd BETTER TO LAUNCH MANUALLY

# exec-once = $terminal
# exec-once = nm-applet &

# Note: Dbus must start fully before pipewire would launch with exec-once
#exec-once = pipewire
#exec-once = pipewire-pulse
# for some apps that require DISPLAY variable
# (system-cringed uses it by default), added sleep time to make sure dbus rocketed
#exec = gentoo-pipewire-launcher restart & disown # This one was exactly made for reminder 
# if i need to restart pipewire
# but just in case
# 
#exec-once = dunst # Can be alternative to mako


#############################
### ENVIRONMENT VARIABLES ###
#############################
env = XCURSOR_THEME,phinger-cursors-light
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = MOZ_ENABLE_WAYLAND, 1 # envvar for firefox to enable wayland
env = QT_QPA_PLATFORMTHEME,qt6ct   # for Qt apps

env = RTC_USE_PIPEWIRE,true # fucked for a long time to figure out screensharing issue
env = KITTY_ENABLE_WAYLAND, 1 # envvar for kitty to enable wayland
env = HY3_STYLE, tab { font-weight: bold; } # some font of hy3 plugin tabs

#####################
### LOOK AND FEEL ###
#####################

general { 
    gaps_in = 5
    gaps_out = 10
    border_size = 4
    col.active_border = rgba(dddaecec) rgba(6e56afaa) 30deg 
    col.inactive_border = rgba(595959aa)
    resize_on_border = false 
    allow_tearing = false
    layout = hy3 # enabled hy3 better tiling
}

decoration {
    rounding = 5
    # active_opacity = 0.98
    # inactive_opacity = 0.88
    dim_inactive = false 
    dim_strength = 0.2
    blur {
        enabled = true
        size = 10
        # passes = 1
        vibrancy = 0.1696

        ignore_opacity = true
        xray = false
        new_optimizations = true
        passes = 4
    }
}


animations {
    enabled = true
    bezier = windowIn, 0.06, 0.71, 0.25, 1
    bezier = windowOut, 0.06, 0.71, 0.25, 1
    bezier = windowResize, 0.04, 0.67, 0.38, 1
    animation = windowsIn, 1, 2, windowIn, slidefade 20%
    animation = windowsOut, 1, 2, windowOut, slidefade 70%
    animation = windowsMove, 1, 2, windowResize, slidefade 50%
    animation = border, 1, 2, default
    animation = borderangle, 1, 2, default
    animation = fade, 1, 2, default
    animation = workspaces, 1, 2, default, slidefade 20%

		animation = layers, 1, 2, windowIn, slidefade 50%
		animation = layers, 1, 2, windowIn
}

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # I probably want this
}

master {
    new_status = master
}

misc { 
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    font_family = "FiraCode Nerd Font"
}

binds {
  workspace_back_and_forth = true
}


#############
### INPUT ###
#############

input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
	# Yes, the shortcut is literally: LSHIFT + RSHIFT
	kb_options = grp:shifts_toggle
    kb_rules =

    follow_mouse = 1

    sensitivity = 0

    repeat_delay = 200
    repeat_rate = 60
}

cursor {
  no_warps = true
  inactive_timeout = 3
}

device {
    name = epic-mouse-v1 # device name specific config
    sensitivity = -0.5
}

group {
  col.border_active = rgba(dddaecec) rgba(f1c4e0aa) 30deg
  col.border_inactive = rgba(595959aa)
  groupbar {
    enabled = false
    font_family = "InconsolataLGCNerdFont"
    font_size = 10
    col.active = rgba(a506a0cc) rgba(191724cc) rgba(191724cc) rgba(191724cc) rgba(191724cc) rgba(191724cc) rgba(191724cc) rgba(191724aa) 30deg
    col.inactive = rgba(1c1c1caa)
    height = 15
    render_titles = true
  }
}

####################
##### PLUGINS ######
####################
# BE careful when you are updating hyprland (will do rarely on gentoo linux) 
# so you will need to rebuild all plugins

plugin {
  hyprexpo {
      columns = 3
      gap_size = 5
      bg_col = rgb(111111)
      # workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1
      workspace_method = first 1

      gesture_distance = 300 # how far is the "max" for the gesture
  }
  hy3 {
        tabs {
            height = 25
	          padding = 5
	          render_text = true
            text_center = true
            text_font = "InconsolataLGCNerdFont-Bold"
            text_height = 12
            col.active = rgba(f5c2e7ec)
            col.focused = rgba(8caaeeec)
            col.inactive = rgba(595959aa)
            col.inactive.text = rgb(ffffaa)
        }
        autotile {
            enable = true
            trigger_width = 800
            trigger_height = 500
        }
    }
}

#########################
### PLUGINS AUTOSTART ###
#########################
exec-once = hyprpm enable hy3
exec-once = hyprpm enable hyprexpo
exec-once = hyprpm reload -n


####################
### KEYBINDINGS ###
####################

$mainMod = SUPER # Sets "Windows" key as main modifier

bind = $mainMod, B, exec, pkill -SIGUSR1 waybar # hide waybar
# bind = $mainMod, W, overview:toggle # overview [i]nformation Hyprspace
bind = $mainMod, W, hyprexpo:expo, toggle
bind = $mainMod, Y, hyprexpo:expo, select

# bind = $mainMod+ALT, L, exec, ~/.config/hypr/scripts/lockscreen.sh # Lock screen
bind = $mainMod+ALT, P, exec, ~/.config/hypr/scripts/lockscreen-aggressive.sh # Lock screen blurring stronger
bind = $mainMod, BACKSPACE, exec, ~/.config/hypr/scripts/powermenu.sh # Display power menu
bind = $mainMod, C, exec, ~/.config/hypr/scripts/keybinds.sh # See keybinds (this window)
# bind = $mainMod, W, exec, bash /home/$USER/.config/hypr/scripts/hyprwindows-see.sh # Windows overview
# bind = $mainMod+SHIFT, W, exec, bash /home/$USER/.config/hypr/scripts/hyprworkspaces-see.sh # Workspaces overview

bind = $mainMod+SHIFT, B, exec, $browser  # Launch a Firefox browser

bind = , Print, exec, hyprshot -m output -o ~/Pictures/Screenshots -D 1 # Make a screenshot (all)
bind = $mainMod, Print, exec, hyprshot -m window -m active -o ~/Pictures/Screenshots -D 1 # Make a screenshot (window)
bind = SHIFT, Print, exec, hyprshot -m region -o ~/Pictures/Screenshots -D 1 # Make a screenshot (area)
bind = , XF86TouchpadToggle, exec, hyprctl -r -q -- keyword device[asue1213:00-04f3:3283-touchpad]:enabled 0 && notify-send "Touchpad off"
bind = $mainMod, XF86TouchpadToggle, exec, hyprctl -r -q -- keyword device[asue1213:00-04f3:3283-touchpad]:enabled 1 && notify-send "Touchpad on"
bind = $mainMod, Return, exec, $terminal # Launch Kitty Terminal
#bind = $mainMod+SHIFT, Return, exec, kgx # Launch Gnome Terminal
bind = $mainMod, U, exec, makoctl restore # [u]nhide last notification (mako)
bind = $mainMod, Y, exec, makoctl dismiss -a # [y]eah i read all notifications (mako)
bind = Alt, F4, hy3:killactive, # Kill active window
bind = $mainMod, V, togglefloating, # Toggle floating mode
bind = $mainMod SHIFT, D, exec, $menu # App launcher
bind = $mainMod Shift, F, exec, rofi  -show find -modi find:~/.local/share/rofi/finder.sh # to find files across the laptop
# bind = $mainMod, N, exec, foot -e nmtui-connect # Manage networks
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, M, togglesplit, # dwindle 
bind = $mainMod, F, fullscreen, # [f]ullscreen
bind = $mainMod, O, pin, # [o]ver all windows and workspaces (always on top)

bind = $mainMod, E, exec, $guiFileManager

# i3-BINDS HERE (THIRD PARTY PLUGIN)
bind = $mainMod, D, hy3:makegroup, h # Prepare to split window horizontally
bind = $mainMod, S, hy3:makegroup, v  # Prepare to split window vertically
bind = $mainMod, G, hy3:makegroup, tab # Create a group
bind = $mainMod, A, hy3:changefocus, raise # Focus multiple windows more (e.g. to create a container)
bind = $mainMod+SHIFT, A, hy3:changefocus, lower # Shrink focus of windows to less
bind = $mainMod, O, hy3:expand, expand # Expand view of current window
bind = $mainMod, P, hy3:expand, shrink # Shrink view of current window
bind = $mainMod+SHIFT, E, hy3:expand, base # restore all windows to their initial sizes
bind = $mainMod, R, hy3:changegroup, opposite # Change window with opposite

# Move focus between floating/tiled windows (super useful)
bind = $mainMod, I, exec, hyprctl dispatch focuswindow $(if [[ $(hyprctl activewindow -j | jq ."floating") == "true" ]]; then echo "tiled"; else echo "floating"; fi;)

# Switch between workspaces
bind = $mainMod, TAB, workspace, previous

# Move focus with hjkl keys
bind = $mainMod, H, hy3:movefocus, l # VIM style movement of focus (left)
bind = $mainMod, J, hy3:movefocus, d # VIM style movement of focus (down)
bind = $mainMod, K, hy3:movefocus, u # VIM style movement of focus (up)
bind = $mainMod, L, hy3:movefocus, r # VIM style movement of focus (right)
bind = $mainMod, left, hy3:movefocus, l  # arrow movement of focus (left)
bind = $mainMod, down, hy3:movefocus, d  # arrow movement of focus (down)
bind = $mainMod, up, hy3:movefocus, u    # arrow movement of focus (up)
bind = $mainMod, right, hy3:movefocus, r # arrow movement of focus (right)

bind = $mainMod, H, bringactivetotop, l # VIM style movement of focus (left)
bind = $mainMod, J, bringactivetotop, d # VIM style movement of focus (down)
bind = $mainMod, K, bringactivetotop, u # VIM style movement of focus (up)
bind = $mainMod, L, bringactivetotop, r # VIM style movement of focus (right)
bind = $mainMod, left, bringactivetotop, l  # arrow movement of focus (left)
bind = $mainMod, down, bringactivetotop, d  # arrow movement of focus (down)
bind = $mainMod, up, bringactivetotop, u    # arrow movement of focus (up)
bind = $mainMod, right, bringactivetotop, r # arrow movement of focus (right)

# switch using mouse
bindn = , mouse:272, hy3:focustab, mouse # Mouse movement
bindn = , mouse_down, hy3:focustab, l, require_hovered # mouse movement
bindn = , mouse_up, hy3:focustab, r, require_hovered # mouse movement

# Move focus
# bind = $mainMod+CONTROL, H, hy3:movefocus, l, visible, nowarp # Focusing windows skipping containers
# bind = $mainMod+CONTROL, J, hy3:movefocus, d, visible, nowarp # Focusing windows skipping containers
# bind = $mainMod+CONTROL, K, hy3:movefocus, u, visible, nowarp # Focusing windows skipping containers
# bind = $mainMod+CONTROL, L, hy3:movefocus, r, visible, nowarp # Focusing windows skipping containers
# bind = $mainMod+CONTROL, left, hy3:movefocus, l, visible, nowarp # Focusing windows skipping containers
# bind = $mainMod+CONTROL, down, hy3:movefocus, d, visible, nowarp # Focusing windows skipping containers
# bind = $mainMod+CONTROL, up, hy3:movefocus, u, visible, nowarp # Focusing windows skipping containers
# bind = $mainMod+CONTROL, right, hy3:movefocus, r, visible, nowarp # Focusing windows skipping containers

# Move window
bind = $mainMod+SHIFT, H, hy3:movewindow, l, once # window movements
bind = $mainMod+SHIFT, J, hy3:movewindow, d, once # window movements
bind = $mainMod+SHIFT, K, hy3:movewindow, u, once # window movements
bind = $mainMod+SHIFT, L, hy3:movewindow, r, once # window movements
bind = $mainMod+SHIFT, left, hy3:movewindow, l, once # window movements
bind = $mainMod+SHIFT, down, hy3:movewindow, d, once # window movements
bind = $mainMod+SHIFT, up, hy3:movewindow, u, once # window movements
bind = $mainMod+SHIFT, right, hy3:movewindow, r, once # window movements

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 01
bind = $mainMod, 2, workspace, 02
bind = $mainMod, 3, workspace, 03
bind = $mainMod, 4, workspace, 04
bind = $mainMod, 5, workspace, 05
bind = $mainMod, 6, workspace, 06
bind = $mainMod, 7, workspace, 07
bind = $mainMod, 8, workspace, 08
bind = $mainMod, 9, workspace, 09
bind = $mainMod, 0, workspace, 10
bind = $mainMod, F1, workspace, 11
bind = $mainMod, F2, workspace, 12
bind = $mainMod, F3, workspace, 13
bind = $mainMod, F4, workspace, 14
bind = $mainMod, F5, workspace, 15
bind = $mainMod, F6, workspace, 16
bind = $mainMod, F7, workspace, 17
bind = $mainMod, F8, workspace, 18
bind = $mainMod, F9, workspace, 19
bind = $mainMod, F10, workspace, 20

# Move active window to a workspace with mainMod + SHIFT + [0-9] and do not focus on window
bind = $mainMod SHIFT, 1, hy3:movetoworkspace, 1
bind = $mainMod SHIFT, 2, hy3:movetoworkspace, 2
bind = $mainMod SHIFT, 3, hy3:movetoworkspace, 3
bind = $mainMod SHIFT, 4, hy3:movetoworkspace, 4
bind = $mainMod SHIFT, 5, hy3:movetoworkspace, 5
bind = $mainMod SHIFT, 6, hy3:movetoworkspace, 6
bind = $mainMod SHIFT, 7, hy3:movetoworkspace, 7
bind = $mainMod SHIFT, 8, hy3:movetoworkspace, 8
bind = $mainMod SHIFT, 9, hy3:movetoworkspace, 9
bind = $mainMod SHIFT, 0, hy3:movetoworkspace, 10
bind = $mainMod+SHIFT, F1, hy3:movetoworkspace, 11
bind = $mainMod+SHIFT, F2, hy3:movetoworkspace, 12
bind = $mainMod+SHIFT, F3, hy3:movetoworkspace, 13
bind = $mainMod+SHIFT, F4, hy3:movetoworkspace, 14
bind = $mainMod+SHIFT, F5, hy3:movetoworkspace, 15
bind = $mainMod+SHIFT, F6, hy3:movetoworkspace, 16
bind = $mainMod+SHIFT, F7, hy3:movetoworkspace, 17
bind = $mainMod+SHIFT, F8, hy3:movetoworkspace, 18
bind = $mainMod+SHIFT, F9, hy3:movetoworkspace, 19
bind = $mainMod+SHIFT, F10, hy3:movetoworkspace, 20

# navigating inside a group
bind = $mainMod+CONTROL, 1, hy3:focustab, index, 01
bind = $mainMod+CONTROL, 2, hy3:focustab, index, 02
bind = $mainMod+CONTROL, 3, hy3:focustab, index, 03
bind = $mainMod+CONTROL, 4, hy3:focustab, index, 04
bind = $mainMod+CONTROL, 5, hy3:focustab, index, 05
bind = $mainMod+CONTROL, 6, hy3:focustab, index, 06
bind = $mainMod+CONTROL, 7, hy3:focustab, index, 07
bind = $mainMod+CONTROL, 8, hy3:focustab, index, 08
bind = $mainMod+CONTROL, 9, hy3:focustab, index, 09
bind = $mainMod+CONTROL, 0, hy3:focustab, index, 10

# moving windows in a grouped tab
bind = $mainMod+CONTROL+SHIFT, H, hy3:movewindow, l, once, visible
bind = $mainMod+CONTROL+SHIFT, J, hy3:movewindow, d, once, visible
bind = $mainMod+CONTROL+SHIFT, K, hy3:movewindow, u, once, visible
bind = $mainMod+CONTROL+SHIFT, L, hy3:movewindow, r, once, visible
bind = $mainMod+CONTROL+SHIFT, left, hy3:movewindow, l, once, visible
bind = $mainMod+CONTROL+SHIFT, down, hy3:movewindow, d, once, visible
bind = $mainMod+CONTROL+SHIFT, up, hy3:movewindow, u, once, visible
bind = $mainMod+CONTROL+SHIFT, right, hy3:movewindow, r, once, visible

# Example special workspace (scratchpad)
bind = $mainMod, T, togglespecialworkspace, tent # Toggle special workspace
bind = $mainMod SHIFT, T, movetoworkspacesilent, special:tent # move to special workspace

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
# Mouse + Keyboard movewindow
bindm = $mainMod, Z, movewindow
bindm = SUPER, X, resizewindow

# move/resize windows with kbd
binde = $mainMod+ALT, h, resizeactive, -100 0 # resize a window
binde = $mainMod+ALT, l, resizeactive, 100 0 # resize a window
binde = $mainMod+ALT, k, resizeactive, 0 -100 # resize a window
binde = $mainMod+ALT, j, resizeactive, 0 100 # resize a window
binde = $mainMod+ALT, left, resizeactive, -100 0 # resize a window
binde = $mainMod+ALT, right, resizeactive, 100 0 # resize a window
binde = $mainMod+ALT, up, resizeactive, 0 -100 # resize a window
binde = $mainMod+ALT, down, resizeactive, 0 100 # resize a window

# Move floating windows with vim keys
binde = $mainMod+CONTROL, h, moveactive, -80 0
binde = $mainMod+CONTROL, l, moveactive, 80 0
binde = $mainMod+CONTROL, j, moveactive, 0 80
binde = $mainMod+CONTROL, k, moveactive, 0 -80
binde = $mainMod+CONTROL, left, moveactive, -80 0
binde = $mainMod+CONTROL, right, moveactive, 80 0
binde = $mainMod+CONTROL, down, moveactive, 0 80
binde = $mainMod+CONTROL, up, moveactive, 0 -80

# Volume Control (can use pactl from pulseaudio or wpctl from wireplumber)
#binde = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
#binde = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+

# I use pactl, because wpctl doesn't support a relative L/R adjusting of volume
binde = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
binde = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
binde = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && notify-send "Audio Toggle Muted/Unmuted"
binde = , XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle && notify-send "Mic Toggle Muted/Unmuted"

# Brightness Control
binde = , XF86MonBrightnessUp, exec, brightnessctl set 5%+
binde = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

# Previous/Next track + Pause/Resume
binde = , XF86AudioPrev, exec, playerctl previous
binde = , XF86AudioNext, exec, playerctl next
binde = , XF86AudioStop, exec, playerctl play-pause

##############################
###### UNUSED BINDINGS #######
##############################

# Binds look and hyprland conf edit commented in favor of free binds
# bind = $mainMod, I, exec, kitty -e nvim -R +/==========HYPR_KEYBINDS========== ~/.config/hypr/hyprland.conf # open the document here to see keybinds
# bind = $mainMod+SHIFT, I, exec, kitty -e nvim ~/.config/hypr/hyprland.conf # open the document for edit
# bindit = $mainMod, SUPER_L, exec, pkill -SIGUSR1 waybar # auto hide waybar
# bindirt = $mainMod, SUPER_L, exec, pkill -SIGUSR1 waybar # auto hide waybar
# bind = $mainMod, M, exit, # in favor of other good opts

# window swapping with keys (hy3 replaces this)
# bind = $mainMod Shift, right, movewindoworgroup, r
# bind = $mainMod Shift, left, movewindoworgroup, l
# bind = $mainMod Shift, up, movewindoworgroup, u
# bind = $mainMod Shift, down, movewindoworgroup, d

# Groups and window cycling inside a group
# bind = $mainMod, G, togglegroup DISABLE IN FAVOR OF hy3:group
# bind = $mainMod+TAB, right, changegroupactive, f # disabled while hy3 grouping is working
# bind = $mainMod+TAB, left, changegroupactive, b # disabled while hy3 grouping is working


#L
# Move focus with mainMod + arrow keys (hy3 replaces this)
# bind = $mainMod, left, movefocus, l
# bind = $mainMod, right, movefocus, r
# bind = $mainMod, up, movefocus, u
# bind = $mainMod, down, movefocus, d
#bind = $mainMod Shift, D, exec, rofi -show {drun,window} -theme ~/rofi-collection/tokyonight/tokyonight.rasi # just in case something fails on wofi (till i find a fix for telegram launching from wofi)
#
##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev = float,class:^(kitty)$,title:^(kitty)$
windowrulev = suppressevent maximize, class:.* # You'll probably like this.
windowrulev = noanim, class:^(wofi)$
windowrulev = noanim, class:^(rofi)$
windowrulev = float, title:^rofi
windowrulev = center, title:^rofi
windowrulev = float, title:foot
windowrulev = float, class:pavucontrol-qt
windowrulev = size 1200 800, class:pavucontrol-qt

windowrulev = noinitialfocus,class:(jetbrains-)(.*),title:^win(.*), initialTitle:win.*, floating:1  

############################
####COLOR SOURCE OVERLAY####
############################

source = ~/.config/hypr/hyprcolors.conf
